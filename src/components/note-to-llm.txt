The solution to this problem is to understand that react pdf renderer uses its own tags that are written starting
with a capital Letter. So the <img> tag in HTML has to be changed to <Img> tag in React PDF renderer.